"""PhotocatalysisPapers — automated index of photocatalysis research papers.

The package is dependency-free (Python standard library only) so the daily
pipeline can run on any machine or CI runner without an install step.
"""

from __future__ import annotations

__version__ = "1.2.1"
